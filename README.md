# 自分OS

自分だけが使う生活補助PWAです。

打刻チェック、有料列車ログ、渋谷ランチログ、趣味メモなど、単体アプリにするほどではない小さな不便を1つのRailsアプリにまとめています。PWA化することで、スマホのホーム画面から1タップで開ける、自分専用アプリのような体験を目指します。

## 機能

- Dashboard: 今日の打刻、有料列車、ランチ、趣味予定をまとめて表示
- 打刻チェック: 今日の出勤・退勤確認と日別履歴
- 有料列車ログ: 月次回数、合計金額、理由、疲労度
- 渋谷ランチログ: 価格、満足度、混雑度、一人利用、再訪フラグ、絞り込み
- 趣味コーナー: 予定とメモをカテゴリ付きで保存
- AIチャットUI: 入力やチップで画面のフォーカスが変わるUIだけを実装
- PWA: manifest、service worker、offlineページ

## セットアップ

```bash
bundle install
bin/rails db:setup
bin/rails server
```

ブラウザで http://localhost:3000 を開きます。

## デプロイ方針

このRails版を将来の本体にする想定です。現在GitHub/Vercelに表示しているNext.js版は、見た目確認とVercel表示用の先行プロトタイプです。

Rails本体をデプロイする場合は、VercelではなくRender、Fly.io、Railway、Heroku系、VPSなどRailsアプリを常駐実行しやすい環境を優先します。フォーム保存、DB、AIチャット連携、認証はこのRails版へ寄せていく方針です。

## PWAとして確認

1. ローカルサーバーを起動します。
2. Chromeなどで http://localhost:3000 を開きます。
3. DevTools の Application タブで Manifest と Service Workers を確認します。
4. スマホ表示幅にすると、下部ナビ付きのアプリ風UIになります。

## 今後やりたいこと

- AIチャットを実際のアプリ操作や提案に接続する
- Web Pushで打刻前通知を出す
- オフライン投稿キューを作る
- ユーザー管理を足して、将来の複数ユーザー化に備える
- 趣味カテゴリごとの専用表示を増やす
